# sources/

Raw captures (transcripts, article text, excerpts) — the immutable first pass of the
two-pass ingestion workflow in [AGENTS.md](../AGENTS.md). Filename:
`<retrieved-date>-<slug>.md` via [templates/source.md](../templates/source.md).
After distillation, append-only (`## Distilled into` list). Not indexed by Logseq
viewers; fully linkable by agents and Obsidian.
